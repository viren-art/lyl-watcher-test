{
  "name": "weather-impact-load-tests",
  "version": "1.0.0",
  "description": "Load testing for BESS optimization",
  "scripts": {
    "test:load": "jest --testMatch='**/load/*.test.js' --runInBand --detectOpenHandles"
  },
  "devDependencies": {
    "autocannon": "^7.12.0",
    "jest": "^29.7.0"
  }
}